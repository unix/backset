import { default as create } from './create'
import { default as useStateRef } from './use-state-ref'

const Backset = {
  create,
  useStateRef,
}

export default Backset
export { create, useStateRef }
